{
  "name": "precious-memory",
  "version": "1.0.0",
  "private": true,
  "description": "A private two-person chat history recorder with Markdown support.",
  "main": "backend/server.js",
  "scripts": {
    "start": "node backend/server.js",
    "dev": "NODE_ENV=development node --watch backend/server.js",
    "check": "node --check backend/server.js && node --check backend/app.js && node --check backend/conversation-store.js && node --check public/app.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "express": "^5.1.0",
    "markdown-it": "^14.1.0",
    "sanitize-html": "^2.17.0"
  }
}
